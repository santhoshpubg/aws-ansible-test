---
- name: install apache on bothe servers
  hosts: webserver
  become: true
  tasks:
    - name : install apache
      apt:
        name : apache2
        state : present
        update_cache : yes
    - name : delete index.html
      file:
        path : /var/www/html/index.html
        state : absent
      notify: restart apache2
  handlers :
    - name : restart apache2
      service :
        name : apache2
        state : restarted

- hosts: 172.31.16.109
  become: true
  tasks:
    - name: set up index.html for first web server
      copy: content="<html><header><title>Welcome to Server 1</title></header><body>Hello from Server 1! ARUNA ARUNA ARUNA</body></html>" dest=/var/www/html/index.html mode=0644
      notify: restart apache2
  handlers:
    - name: restart apache2
      service: name=apache2 state=restarted

- hosts: 172.31.29.227
  become: true
  tasks:
    - name: set up index.html for first web server
      copy: content="<html><header><title>Welcome to Server 2</title></header><body>Hello from Server 2! DEENDAYAL DEENDAYAL DEENDAYAL</body></html>" dest=/var/www/html/index.html mode=0644
      notify: restart apache2
  handlers:
    - name: restart apache2
      service: name=apache2 state=restarted
